import FlowApp from "./FlowApp";

// Note: The previous version of this file contained a fetch() POST "send" flow
// to a local backend. Sophie deleted that part, can be found in Imos git history if needed.
// (WebSocket integration can be added later.)

export default function App() {
  return <FlowApp />;
}
